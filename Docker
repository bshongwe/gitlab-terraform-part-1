# Node.js base image
FROM node:16

# Create app directory
WORKDIR /usr/src/app

# Copy pkg & pkg-lock JSONs
COPY package*.json

# Install dependencies
RUN npm install

# Copy the rest of the app
COPY ..

# Expose app port permission
EXPOSE 3000

# Run app commands
CMD [ "npm", "start" ]
